% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assemble_user_evidence.R
\name{assemble_user_evidence}
\alias{assemble_user_evidence}
\title{Assemble user multi-omics inputs into a per-gene evidence table}
\usage{
assemble_user_evidence(
  rna_fit = NULL,
  prot_fit = NULL,
  scrna_summary = NULL,
  serum_summary = NULL,
  signal_peptide = NULL,
  cross_cohort_agreement = NULL,
  max_I2 = NULL
)
}
\arguments{
\item{rna_fit}{Optional output of \code{\link[=classify_trajectory]{classify_trajectory()}} (i.e., a
data.table with \code{gene_symbol}, \code{rna_pattern},
\code{rna_pattern_rho}, \code{lrt_padj}).}

\item{prot_fit}{Optional output of \code{\link[=classify_protein_trajectory]{classify_protein_trajectory()}}
(a data.table with \code{gene_symbol}, \code{prot_pattern},
\code{prot_pattern_rho}).}

\item{scrna_summary}{Optional data.table with \code{gene_symbol} and
\code{cell_origin_top} columns. Cell-of-origin label per gene.}

\item{serum_summary}{Optional data.table with \code{gene_symbol},
\code{serum_detected} (logical), and \code{serum_log2fc_PDAC_vs_HC}
(numeric).}

\item{signal_peptide}{Optional character vector of gene symbols
that carry a signal peptide (UniProt SP-positive). Genes outside
this set are flagged \code{flt_signal_peptide = FALSE}.}

\item{cross_cohort_agreement}{Optional named numeric vector,
names = gene symbols, values in \verb{[0, 1]} representing fraction
of cohorts agreeing on direction. When \code{NULL}, defaults to
\code{1.0} for genes with \code{rna_pattern} set and \code{NA} otherwise.}

\item{max_I2}{Optional named numeric vector, names = gene
symbols, values are max meta-analysis I². When \code{NULL}, defaults
to \code{0} (no heterogeneity penalty).}
}
\value{
A \code{data.table} with the column schema consumed by
\code{compute_audit_score(evidence = ...)}.
}
\description{
Combines optional per-layer user inputs into a single per-gene
evidence data.table whose column schema is the subset of the
bundled \code{pdactrace_reference} atlas that
\code{compute_audit_score(evidence = ...)} consumes. Layers the user
does not supply are filled with \code{NA}; the downstream feature
constructor in \code{.audit_compute_features()} is NA-tolerant and
degrades gracefully (a layer absent → that layer contributes
zero to \code{evidence_strength}; cross-layer concordance falls to
the available pair; etc.).
}
\examples{
rna_fit <- data.table::data.table(
  gene_symbol = c("FAKE_UP", "FAKE_DOWN"),
  rna_pattern = c("Early_Burst_Up", "Early_Loss_Down"),
  rna_pattern_rho = c(0.95, 0.92),
  lrt_padj = c(1e-4, 1e-3))
ev <- assemble_user_evidence(rna_fit = rna_fit)
compute_audit_score(evidence = ev)
}
