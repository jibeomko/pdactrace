% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evidence_model.R
\name{explain_user_evidence_model}
\alias{explain_user_evidence_model}
\title{Explain a \code{pdactrace_user_model} via its top contributing features}
\usage{
explain_user_evidence_model(model, top_n = 10L, verbose = TRUE)
}
\arguments{
\item{model}{A \code{pdactrace_user_model} from
\code{\link[=fit_user_evidence_model]{fit_user_evidence_model()}}.}

\item{top_n}{Number of positive AND number of negative
coefficients to surface. Default \code{10}.}

\item{verbose}{Logical. If \code{TRUE} (default), prints to console.}
}
\value{
Invisibly, a list with \code{top_pos}, \code{top_neg},
\code{cv_auc_mean}, \code{cv_auc_sd}, \code{lambda_min}, \code{n_features},
\code{n_train}, \code{n_positives}.
}
\description{
Prints (and invisibly returns) the top-N positive and top-N
negative coefficients of the fitted elastic-net model, with
feature names and signed coefficient values. This is the
user-facing rationale for the supervised score: which evidence
axes pushed the user's positive set up.
}
\examples{
if (requireNamespace("glmnet", quietly = TRUE)) {
  set.seed(1)
  feats <- data.frame(gene_symbol = paste0("G", seq_len(30)),
                      x1 = rnorm(30), x2 = rnorm(30))
  y <- rep(c(0L, 1L), each = 15)
  fit <- fit_user_evidence_model(feats, y, nfolds = 3L)
  explain_user_evidence_model(fit, top_n = 2, verbose = FALSE)
}

\donttest{
  feats <- make_evidence_features(scale = "z",
                                    drop_na_rows = TRUE)
  y <- as.integer(feats$gene_symbol \%in\%
                    c("LGALS3BP", "TIMP1", "LRG1"))
  fit <- fit_user_evidence_model(feats, y)
  explain_user_evidence_model(fit, top_n = 5)
}
}
\seealso{
\code{\link[=model_card]{model_card()}} for the full reviewer-facing card.
}
