% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evidence_math.R
\name{evidence_math}
\alias{evidence_math}
\title{Per-axis mathematical evidence for one gene}
\usage{
evidence_math(gene_symbol, reference = NULL)
}
\arguments{
\item{gene_symbol}{HGNC gene symbol (length-1 character).}

\item{reference}{Optional \code{data.table} to inject in place of
the bundled atlas (used by tests and downstream pipelines).}
}
\value{
Invisibly, a list with one element per axis (named-list
of named scalars). Suitable for \code{str()} / programmatic use.
The full 12-rho vector for non-Early best-match genes can be
recovered with \code{\link[=score_trajectory]{score_trajectory()}} using the bundled fit.
}
\description{
Returns the underlying numerical evidence values that fed the
frozen v0.3.0 audit decisions, organised by axis. This is the
canonical machine-readable form sitting between the plain-English
phase-tag layer (\code{\link[=format_provenance]{format_provenance()}}) and the audit-score
decomposition (\code{\link[=explain_score]{explain_score()}}). The point is to expose
\emph{what the math actually says} -- not just labels, not just a
composite score.
}
\details{
Every value reported here is either read directly from
\link{pdactrace_reference} (or \link{pdactrace_protein_betas} for the
protein side) or derived by a single named arithmetic operation
on those columns. No new fitting, no recomputation. The function
name \code{evidence_math} reflects that it is the mathematical view
of the same evidence captured by the verbose / provenance text
layers.
}
\section{Axes}{


\describe{
\item{\code{trajectory_fit}}{Best 12-template Pearson rho, runner-up
rho, and the specificity margin \code{delta_rho = rho_best - rho_runner_up}. A delta_rho above ~0.10 indicates clean
template specificity (the gene's profile is closer to its
matched template than to any other).}
\item{\code{effect_magnitude}}{Euclidean norm and max-absolute of the
per-stage beta vector, on both RNA and protein sides. The
norm uses the 3-vector \verb{(beta_E, beta_M, beta_L)} because
\code{beta_N = 0} is a reference-level constant by DESeq2 / limma
convention, not a measured effect.}
\item{\code{cohort_consistency}}{Stouffer Z, BH-adjusted p, fraction
of contributing cohorts agreeing with the meta sign, and the
maximum heterogeneity I-squared across the three contrasts
N-vs-E, M-vs-E, L-vs-E (which feeds the heterogeneity gate).}
\item{\code{rna_protein_coupling}}{Cosine similarity between the
RNA and tissue-protein beta vectors over \verb{(E, M, L)}.
\code{+1} means same template + same direction; \code{-1} means
opposite. Reported alongside the categorical \code{prot_tier}
and the boolean \code{rnaprot_concordant} flag.}
\item{\code{serum_bridge}}{Translation class A / B / C
(A = same-direction RNA<->serum, B = opposite, C =
decoupled), serum log2 fold-changes against healthy and
pancreatitis cohorts, number of detected cohorts, and the
\code{phase77_strict} membership flag.}
\item{\code{cell_specificity}}{Top scRNA cell of origin and the
Yanai et al. tau index. tau approaches 1 when expression
is concentrated in one cell type.}
\item{\code{filter_survival}}{The 7-step phase60 audit as a count
of passed steps plus a per-step boolean vector
(signal_peptide, serum_measurable, serum_significant,
pancreatitis_pdac, pancreatitis_hc, direction_match, final).}
\item{\code{clinical_role}}{Whether the gene was screened in the
phase29 resectable-marker shortlist and whether it is a
member of the predeclared serum panel.}
}
}

\examples{
m <- evidence_math("LGALS3BP")
m$trajectory_fit
m$rna_protein_coupling$cosine
m$filter_survival$passed
}
\seealso{
\code{\link[=explain_gene]{explain_gene()}} for the formatted text view, and
\code{\link[=compare_genes]{compare_genes()}} for a multi-gene tidy table.
}
