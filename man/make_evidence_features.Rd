% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evidence_features.R
\name{make_evidence_features}
\alias{make_evidence_features}
\title{Build a flat numeric feature matrix for downstream modelling}
\usage{
make_evidence_features(
  genes = NULL,
  reference = NULL,
  protein_betas = NULL,
  scale = c("none", "z", "robust"),
  impute = c("none", "mean", "zero"),
  drop_na_rows = FALSE
)
}
\arguments{
\item{genes}{Optional character vector of gene symbols. \code{NULL}
(default) returns features for every gene in the reference.}

\item{reference}{Optional \code{data.table} to inject in place of the
bundled atlas (used by tests and downstream pipelines).}

\item{protein_betas}{Optional \code{data.table} to inject in place of
the bundled protein-betas table (used by tests).}

\item{scale}{One of \code{"none"} (default), \code{"z"}, or \code{"robust"}.}

\item{impute}{One of \code{"none"} (default), \code{"mean"}, or \code{"zero"}.
Most genes are missing from \code{pdactrace_protein_betas} and / or
from the bundled serum cohorts; the resulting \code{NA}s leave most
genes incomplete-case. Use \code{"mean"} to fill \code{NA} with the
column mean (over complete cases) -- standard pre-fit hygiene
for \code{glmnet}. Use \code{"zero"} to fill \code{NA} with 0 -- conservative
"no evidence" assumption, useful when the absence of an
evidence layer is itself meaningful.}

\item{drop_na_rows}{Logical. If \code{TRUE}, drop rows containing
any \code{NA} in feature columns. Default \code{FALSE}. Applied \strong{after}
\code{impute}, so \code{impute = "mean" + drop_na_rows = FALSE} is
typically what you want for ML fitting.}
}
\value{
A \code{data.table} keyed on \code{gene_symbol} with one row per
gene and one column per feature.
}
\description{
Returns a wide numeric \code{data.table} (genes by features) suitable
for off-the-shelf supervised or descriptive modelling. Computes
the same arithmetic that \code{\link[=evidence_math]{evidence_math()}} exposes per-axis, but
as a single tabular shape so callers can hand it directly to
\code{glmnet::cv.glmnet()}, \code{ranger::ranger()}, scikit-learn, etc.
}
\details{
Feature columns are deterministic functions of the bundled
\link{pdactrace_reference} and \link{pdactrace_protein_betas} objects; no
new fitting is performed. The full column list is grouped below
by evidence axis.
}
\section{Feature columns}{


\describe{
\item{Trajectory fit}{\code{trajectory_rho_best},
\code{trajectory_delta_rho} (= \code{rho_best - rho_runner_up}).}
\item{Effect magnitude}{\code{rna_beta_norm}
(= sqrt(sum(rna_beta_E,M,L^2))), \code{rna_beta_max_abs},
\code{prot_beta_norm}, \code{prot_beta_max_abs}.}
\item{Cohort robustness}{\code{cohort_agreement},
\code{stouffer_abs_z}, \code{stouffer_neglog10_fdr},
\code{max_meta_I2}.}
\item{RNA-protein convergence}{\code{rna_protein_cosine}
(cosine of (E,M,L) beta vectors),
\code{rna_protein_concordant} (0/1),
\code{prot_pattern_rho}.}
\item{Serum bridge}{\code{serum_detected} (0/1),
\code{serum_abs_log2fc}, \code{serum_n_cohorts_detected},
\code{phase77_strict} (0/1).}
\item{Specificity}{\code{cell_specificity_tau},
\code{filter_pass_fraction} (passed / 7).}
\item{Leakage flags}{\code{leakage_housekeeping} (0/1),
\code{leakage_plasma_high_abundance} (0/1).}
}

Plus one identifier column (\code{gene_symbol}). Logical inputs are
coerced to integer 0/1 so the resulting frame is uniformly
numeric except for the key.
}

\section{Scaling}{

\code{scale = "z"} standardizes each feature column to mean 0, sd 1
over the \strong{complete-case} subset (NAs are preserved as NA and
do not contribute to the moment estimates). \code{scale = "robust"}
uses median / MAD instead. \code{scale = "none"} (default) returns
raw values.
}

\section{NA policy}{

Genes missing from \link{pdactrace_protein_betas} get \code{NA} for the
protein-side columns (\verb{prot_beta_*}, \code{rna_protein_cosine},
\code{prot_pattern_rho}). Genes with \code{NA} \code{rna_pattern} (no Early
best-match on the atlas surface) get \code{NA} for
\code{trajectory_rho_best} and \code{trajectory_delta_rho}. Set
\code{drop_na_rows = TRUE} to keep only complete-case rows; default
\code{FALSE} preserves the full atlas row count.
}

\examples{
feats <- make_evidence_features()
dim(feats)
head(feats)
z <- make_evidence_features(scale = "z", drop_na_rows = TRUE)
summary(z[, -1L])
}
\seealso{
\code{\link[=evidence_math]{evidence_math()}} for the per-axis nested-list view,
\code{\link[=score_anchor_similarity]{score_anchor_similarity()}} for descriptive feature-space
scoring against bundled anchors, \code{\link[=fit_user_evidence_model]{fit_user_evidence_model()}}
for the user-supplied supervised wrapper.
}
