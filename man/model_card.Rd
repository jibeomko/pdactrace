% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_card.R
\name{model_card}
\alias{model_card}
\title{Model card metadata for the optional ML layer}
\usage{
model_card(
  source = c("anchor_similarity", "user_model"),
  model = NULL,
  verbose = TRUE
)
}
\arguments{
\item{source}{One of \code{"anchor_similarity"} or \code{"user_model"}.}

\item{model}{Optional \code{pdactrace_user_model} from
\code{\link[=fit_user_evidence_model]{fit_user_evidence_model()}} (required when
\code{source = "user_model"}).}

\item{verbose}{Logical. If \code{TRUE} (default), prints the card to
the console. The structured list is returned invisibly either
way.}
}
\value{
Invisibly, a list with at least: \code{source},
\code{feature_set_version}, \code{intended_use}, \code{not_intended_for},
\code{labels_used_for_training}, \code{limitations}. For
\code{"user_model"}, also includes \code{n_features}, \code{n_train},
\code{n_positives}, \code{cv_auc_mean}, \code{cv_auc_sd}, \code{top_pos},
\code{top_neg}, \code{seed}, \code{alpha}, \code{lambda_min}.
}
\description{
Single point of documentation reviewers can audit when a
descriptive (\code{\link[=score_anchor_similarity]{score_anchor_similarity()}}) or supervised
(\code{\link[=fit_user_evidence_model]{fit_user_evidence_model()}}) score is in use. Returns a
structured list and prints a multi-line summary to the console.
}
\details{
Two sources are supported:
\describe{
\item{\code{"anchor_similarity"}}{The descriptive layer. No labels
are used to train a predictor; the bundled
\link{pdactrace_external_anchors} table is used \strong{only} to
define a reference centroid in the z-scored feature space.
Always available, no \code{model} argument needed.}
\item{\code{"user_model"}}{The user-supplied supervised fit
produced by \code{\link[=fit_user_evidence_model]{fit_user_evidence_model()}}. Pass the fit
object via \code{model = ...}.}
}

The package \strong{ships no pretrained predictor}. This deliberate
constraint is the central defense against the
"what was the training label?" reviewer question for PDAC
biomarker discovery, where validated non-circular ground truth
is not available.
}
\examples{
model_card("anchor_similarity")
\dontrun{
  if (requireNamespace("glmnet", quietly = TRUE)) {
    feats <- make_evidence_features(scale = "z",
                                      drop_na_rows = TRUE)
    y <- as.integer(feats$gene_symbol \%in\%
                      c("LGALS3BP", "TIMP1", "LRG1"))
    fit <- fit_user_evidence_model(feats, y)
    model_card(source = "user_model", model = fit)
  }
}
}
\seealso{
\code{\link[=score_anchor_similarity]{score_anchor_similarity()}},
\code{\link[=fit_user_evidence_model]{fit_user_evidence_model()}}.
}
