% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query_gene_detailed.R
\name{query_gene_detailed}
\alias{query_gene_detailed}
\title{Detailed gene-level evidence - 5 granularity tables}
\usage{
query_gene_detailed(gene_symbol)
}
\arguments{
\item{gene_symbol}{HGNC standard symbol (case-sensitive).}
}
\value{
Named list with 5 slots (each a \code{data.table}) plus
\verb{$summary} (1-line text) and \verb{$provenance}. Returns \code{NULL}
invisibly with a message if \code{gene_symbol} is not in the atlas.
}
\description{
Returns a named list of \code{data.table}s exposing the \emph{hidden} per-stage,
per-cohort, per-celltype, per-filter-step, and per-serum-cohort detail
that \code{query_gene()} summarizes only at the headline level.
}
\details{
Use this for reviewer-grade audit (e.g., \emph{"is the LTBP1 upregulation
consistent across all 4 RNA cohorts?"}) or for downstream computation.
}
\section{Slots}{

\itemize{
\item \strong{per_stage} - 4 rows (Normal, Early, Mid, Late) with
\code{log2FC}, \code{lfcSE}, \code{ci_lo}, \code{ci_hi}, \code{lrt_padj}, \code{wald_padj},
\code{significant}. \code{lrt_padj} is the omnibus DESeq2 LRT padj
(identical across stages by construction); \code{wald_padj} is the
per-contrast Wald padj from a \code{nbinomWaldTest()} refit of the
same model (v0.4.0). The \code{significant} column uses Wald padj.
\item \strong{per_cohort} - 4 rows (TCGA, CPTAC, GSE224564, GSE79668) with
\code{trend} (-1/0/+1), \code{monotonic} (lgl), plus Stouffer summary as
table attribute.
\item \strong{per_celltype} - 11 rows with \code{mean_expression}, \code{pct_of_total},
ranked descending.
\item \strong{filter_diag} - 7 rows (phase60 steps) with \code{pass} (lgl),
\code{underlying_metric}, \code{cutoff}, \code{note}.
\item \strong{serum_per_cohort} - 3 rows (PDAC, Pancreatitis, HC) with
\code{mean}, \code{n_unknown} (cohort sample n not always available),
plus PDAC-vs-Pan t-test p-value as attribute.
}
}

\examples{
  d <- query_gene_detailed("LTBP1")
  d$per_stage      # log2FC + 95\% CI per stage
  d$per_cohort     # cohort x trend x monotonic
  d$filter_diag    # 7-step audit with underlying numbers
}
