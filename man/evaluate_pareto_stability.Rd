% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pareto_audit.R
\name{evaluate_pareto_stability}
\alias{evaluate_pareto_stability}
\title{Evaluate Monte-Carlo Pareto stability under perturbed evidence}
\usage{
evaluate_pareto_stability(
  atlas = NULL,
  n_draws = 1000L,
  axes = c("audit_evidence_strength", "audit_biological_coherence",
    "audit_translational_relevance"),
  gate_cols = c("audit_leakage_gate", "audit_heterogeneity_gate"),
  score_col = "audit_score",
  top_n = 2000L,
  seed = 20250506L
)
}
\arguments{
\item{atlas}{Optional atlas (see \code{\link[=compute_pareto_layers]{compute_pareto_layers()}}).}

\item{n_draws}{Integer number of Monte Carlo draws. Defaults to
\code{1000L}, matching the bundled MC summary.}

\item{axes}{Character(3) axis columns; see \code{\link[=compute_pareto_layers]{compute_pareto_layers()}}.}

\item{gate_cols}{Character(2) gate columns.}

\item{score_col}{Column used to restrict the candidate pool to the
top \code{top_n} rows by deterministic ranking, per draw.}

\item{top_n}{Integer pool size; see \code{\link[=compute_pareto_layers]{compute_pareto_layers()}}.}

\item{seed}{Random seed.}
}
\value{
A data.table with one row per gene in \code{atlas}, with columns
\code{pareto_layer_median}, \code{pareto_stability_top1},
\code{pareto_layer_lo95}, \code{pareto_layer_hi95},
\code{pareto_top10_pct_stability}. Genes outside the gate-passing pool
in every draw receive \code{NA} median/CI but a stability of \code{0}.
}
\description{
\code{evaluate_pareto_stability()} recomputes the Pareto layer assignment
across \code{n_draws} Monte Carlo perturbations of the underlying
evidence (pattern-correlation rho, cohort agreement, and
between-cohort I^2), using the same perturbation kernel as
\code{\link[=propagate_uncertainty]{propagate_uncertainty()}}. For each gene it returns the proportion
of draws in which the gene appears in Pareto layer 1 of the
gate-passing candidate pool. This quantifies axis-agnostic ranking
robustness — a complement to the score-space CI returned by
\code{\link[=propagate_uncertainty]{propagate_uncertainty()}}.
}
\details{
This function is the MC analogue of \code{\link[=compute_pareto_layers]{compute_pareto_layers()}}; it
does not modify the frozen audit score in any draw.
}
\examples{
head(evaluate_pareto_stability(n_draws = 10L, top_n = 50L))

\donttest{
  evaluate_pareto_stability(n_draws = 50L)
}
}
