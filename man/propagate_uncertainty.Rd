% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit_score.R
\name{propagate_uncertainty}
\alias{propagate_uncertainty}
\title{Propagate audit-score uncertainty}
\usage{
propagate_uncertainty(gene_symbol, n_mc = NULL, seed = 42, evidence = NULL)
}
\arguments{
\item{gene_symbol}{Character vector of HGNC gene symbols.}

\item{n_mc}{Optional integer number of Monte Carlo iterations.}

\item{seed}{Random seed used when \code{n_mc} is supplied.}

\item{evidence}{Optional user-supplied evidence table (see
\code{\link[=assemble_user_evidence]{assemble_user_evidence()}}). When \code{NULL} (default), the bundled
reference atlas is used. When supplied without \code{n_mc}, the
stored MC summary is unavailable and \code{n_mc} must be set.}
}
\value{
A data.table with score CI, rank CI, uncertainty width, and class.
}
\description{
By default this returns the stored Monte Carlo uncertainty summary from
the atlas. If \code{n_mc} is supplied, the uncertainty is recomputed over the
full atlas using the frozen scoring rule and perturbing evidence values
only.
}
\examples{
propagate_uncertainty(c("LTBP1", "THBS2", "GAPDH"))
}
