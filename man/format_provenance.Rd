% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_provenance.R
\name{format_provenance}
\alias{format_provenance}
\title{Human-readable evidence labels for the bundled phase tags}
\usage{
format_provenance(provenance, style = c("compact", "verbose", "raw"))
}
\arguments{
\item{provenance}{Character of length 1 or longer. Either a
comma-separated phase string (e.g. \code{"phase33,phase34,phase60"})
or a character vector of individual tags. Unknown tags fall
through unchanged with a \code{(unmapped)} suffix in \code{verbose} mode.}

\item{style}{One of:
\itemize{
\item \code{"compact"} (default) — returns a one-line plus-separated
summary suitable for inline print, e.g.
\code{"RNA + Tissue protein + Multi-cohort RNA + ..."}.
\item \code{"verbose"} — returns a multi-line hyphen-prefixed list with
"label: explanation" per evidence source.
\item \code{"raw"} — returns the input phase tags themselves (useful for
the technical-provenance footer).
}}
}
\value{
Character of length 1.
}
\description{
Maps the internal \code{phase33}, \code{phase34}, \code{stouffer_consistency}, ...
provenance tags carried in \code{pdactrace_reference$provenance} onto
plain-English evidence-source labels. The phase tags remain the
canonical IDs for reproducibility (they correspond to the numbered
analysis scripts in the companion manuscript-monorepo) but the
user-facing print path now leads with the labels and folds the raw
tag list into a \verb{Technical:} footer.
}
\details{
Intended for internal use by \code{\link[=query_gene]{query_gene()}},
\code{\link[=query_gene_detailed]{query_gene_detailed()}}, and \code{\link[=summarize_gene_evidence]{summarize_gene_evidence()}} —
exported so end users who consume the raw \code{provenance} column can
relabel it the same way.
}
\examples{
format_provenance("phase33,phase34,phase60", style = "compact")
format_provenance("phase33,phase34,phase60", style = "verbose")
format_provenance("phase33,phase34,phase60", style = "raw")
}
