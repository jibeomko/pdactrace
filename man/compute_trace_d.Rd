% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trace_d.R
\name{compute_trace_d}
\alias{compute_trace_d}
\title{Algorithm 1 — TRACE-D: Tissue-to-serum Directional Evidence Concordance}
\usage{
compute_trace_d(
  atlas = NULL,
  tau_tissue = 0.5,
  tau_serum = 0.1,
  weights = c(tissue = 0.5, serum = 0.3, specificity = 0.2),
  legacy_translation = c("strict", "fallback")
)
}
\arguments{
\item{atlas}{Optional atlas (defaults to the bundled
\code{pdactrace_reference}). Must contain \code{gene_symbol}, \code{rna_pattern},
\code{prot_pattern}, \code{max_abs_beta_meta}, \code{serum_log2fc_PDAC_vs_HC},
\code{serum_log2fc_Pan_vs_HC}, \code{serum_detected}, \code{flt_signal_peptide}.}

\item{tau_tissue}{Numeric; minimum \verb{|max_abs_beta_meta|} for a
tissue signal to count. Default \code{0.5}.}

\item{tau_serum}{Numeric; minimum \verb{|serum_log2fc_PDAC_vs_HC|} for a
serum signal to count. Default \code{0.1}.}

\item{weights}{Named numeric vector of length 3 summing to 1
(tissue, serum, specificity components of \code{tracd_confidence}).
Defaults to \code{c(tissue = 0.50, serum = 0.30, specificity = 0.20)}.}

\item{legacy_translation}{One of \code{"strict"} or \code{"fallback"}. In
\code{"strict"} mode, TRACE-D uses only measured serum log2FC and the
thresholds above. In \code{"fallback"} mode, rows with no strict TRACE-D
call can inherit the legacy atlas \code{translation_class} A/B/C as a
compatibility annotation; confidence then receives tissue-only
credit and the decision path records \code{legacy_translation}.}
}
\value{
A data.table with one row per gene in \code{atlas} and the
columns \code{tracd_tissue_dir}, \code{tracd_serum_dir}, \code{tracd_class},
\code{tracd_confidence}, \code{tracd_pancreatitis_overlap_score},
\code{tracd_pancreatitis_specificity}, \code{tracd_tissue_weight},
\code{tracd_decision_path}.
}
\description{
\code{compute_trace_d()} deterministically types each gene by the direction
concordance between tissue (RNA + Protein) and serum evidence, with
an explicit pancreatitis-specificity annotation that separates
PDAC-specific signal from shared-inflammation signal. The four
classes ("A" direction-preserved, "B" direction-inverted, "C"
decoupled, \code{NA} insufficient tissue evidence) coexist with the
legacy \code{translation_class} column rather than replacing it
(back-compatible).
}
\details{
This is one of two domain-specific audit algorithms introduced in
pdactrace v0.99.19; see \code{\link[=compute_pareto_layers]{compute_pareto_layers()}} for Algorithm 2.
Neither algorithm introduces new statistical machinery — the
contribution is the domain-specific combination of direction
consensus, magnitude thresholds, and pancreatitis-specificity
annotation packaged as a deterministic, reproducible audit layer.

Pancreatitis-specificity is \strong{annotation only} by design: a
Class A candidate flagged \code{shared_inflammation} is not demoted —
downstream consumers apply stricter filters via
\code{\link[=list_candidates]{list_candidates()}} if required.
}
\examples{
head(compute_trace_d())
compute_trace_d()[gene_symbol \%in\% c("LGALS3BP", "LTBP1", "GAPDH")]
}
