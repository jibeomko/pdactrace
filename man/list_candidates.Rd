% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list_candidates.R
\name{list_candidates}
\alias{list_candidates}
\title{List candidate genes by biology-level filter parameters}
\usage{
list_candidates(
  onset = NULL,
  tissue_direction = NULL,
  pattern = NULL,
  translation_class = NULL,
  serum_detected = NULL,
  signal_peptide = NULL,
  panel_member = NULL,
  resectable_marker = NULL,
  phase77_strict = NULL,
  min_audit_class = c("ALL", "high_confidence", "supported_uncertain", "penalized",
    "low", "excluded"),
  top_n = NULL
)
}
\arguments{
\item{onset}{Character. \code{"Early"} to restrict to the 4 Early-onset
patterns (the surfaced atlas universe). \code{NULL} (default) =
unconstrained, which equals onset = "Early" because the atlas
surface only exposes Early × 4 calls (under the v0.4.0 12-template
classifier, Mid / Late / Monotonic best-matches are flagged via
\verb{excluded_*_pattern} columns and not returned by this function).}

\item{tissue_direction}{Character. \code{"Up"} keeps \code{Early_Burst_Up} +
\code{Early_Peak}; \code{"Down"} keeps \code{Early_Loss_Down} + \code{Early_Trough}.
\code{NULL} = both.}

\item{pattern}{Character vector. Specific 4-Early pattern names
(e.g., \code{c("Early_Burst_Up", "Early_Peak")}). Overrides
\code{tissue_direction} if both are set.}

\item{translation_class}{Character. One of \code{"concordant"} (Class A),
\code{"inverse"} (Class B), \code{"decoupled"} (Class C). Aliases \code{"A"},
\code{"B"}, \code{"C"} are also accepted. \code{NULL} = any.}

\item{serum_detected}{Logical. Restrict to genes detected (or not)
in any serum cohort. \code{NULL} = any.}

\item{signal_peptide}{Logical. Filter on \code{flt_signal_peptide}
(phase60 SignalP pass). \code{NULL} = any.}

\item{panel_member}{Logical. Member of LTBP1+SERPINA1 / hybrid
panels. \code{NULL} = any.}

\item{resectable_marker}{Logical. Member of phase29 25 markers (19
in atlas). \code{NULL} = any.}

\item{phase77_strict}{Logical. Member of 22 strict tissue+serum
convergent candidates. \code{NULL} = any.}

\item{min_audit_class}{Minimum \code{audit_class} to retain. One of
\code{"high_confidence"}, \code{"supported_uncertain"}, \code{"penalized"},
\code{"low"}, \code{"excluded"}, or \code{"ALL"} (no filter). Defaults to
\code{"ALL"}. Hierarchy (high to low):
high_confidence, supported_uncertain, penalized, low, excluded.}

\item{top_n}{Optional integer. Return top N rows ranked by
\code{audit_score} descending.}
}
\value{
\code{data.table} with \code{gene_symbol}, key evidence columns, and
\code{provenance}.
}
\description{
Multi-parameter filter on the pdactrace reference atlas. Pass \code{NULL}
(default) for any parameter to leave it unconstrained. Returns a
ranked \code{data.table} with key evidence columns.
}
\examples{
  list_candidates(onset = "Early", tissue_direction = "Up")
  list_candidates(translation_class = "inverse")
  list_candidates(translation_class = "concordant",
                  serum_detected = TRUE,
                  signal_peptide = TRUE,
                  top_n = 20)
}
