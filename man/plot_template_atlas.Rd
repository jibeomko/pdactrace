% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_template_atlas.R
\name{plot_template_atlas}
\alias{plot_template_atlas}
\title{Per-template trajectory atlas plots}
\usage{
plot_template_atlas(
  layer = c("rna", "protein"),
  templates = NULL,
  output_dir = NULL,
  filename_pattern = "template_{layer}_{template}.pdf",
  width = 1.55,
  height = 1.4,
  reference = NULL
)
}
\arguments{
\item{layer}{Either \code{"rna"} (default) or \code{"protein"}. The protein
layer reads the bundled \link{pdactrace_protein_betas} (5,917
measurable proteins).}

\item{templates}{Optional character vector of template names to
restrict the output to. Default \code{NULL} returns all 12.}

\item{output_dir}{Optional directory. If non-NULL, one cairo PDF
per template is written via \code{\link[=pdactrace_save]{pdactrace_save()}} and the file
paths are reported in \code{attr(out, "files")}. Default \code{NULL}
returns ggplot objects only.}

\item{filename_pattern}{File-name template containing
\code{{layer}} and \code{{template}} placeholders. Default
\code{"template_{layer}_{template}.pdf"}.}

\item{width, height}{Output PDF size in inches. Defaults match the
compact \code{fig2C} style (1.55 x 1.40 in; ~ 4 cm square).}

\item{reference}{Optional override of the bundled
\code{pdactrace_reference} (RNA layer) for unit-test injection. The
protein layer always reads \code{pdactrace_protein_betas}.}
}
\value{
A named \code{list} of \code{ggplot} objects, one per template, in
\code{c(early_pattern_names(), mid_pattern_names_excluded(), "Late_Burst_Up", "Late_Loss_Down", "Monotonic_Up", "Monotonic_Down")} order. When \code{output_dir} is non-NULL, the
list also carries the file paths in \code{attr(out, "files")}.
}
\description{
Renders one panel per trajectory template in the bundled
12-template catalogue. Each panel shows the cohort of
genes / proteins whose z-scored Normal / Early / Mid / Late
trajectory matches that template best (Pearson rho argmax across
the 12 templates; deterministic given the bundled atlas), as thin
translucent lines plus a mean line and a +/-1 SD ribbon. Reproduces
the figures in
\code{analysis/manuscript/PDAC_졸업v1/fig2/individual/fig2C_RNA_*.pdf}
inside the package and extends them to the four templates that
did not exist in the v0.3.0 era (Late_Burst_Up, Late_Loss_Down,
Monotonic_Up, Monotonic_Down).
}
\details{
Cohort assignment is recomputed inside the package via the same
z-scored Pearson-rho argmax that backs \code{\link[=classify_trajectory]{classify_trajectory()}} and
\code{\link[=score_trajectory]{score_trajectory()}}; the bundled atlas's \code{rna_pattern} column
surfaces only the four Early templates, so the eight non-Early
panels rely on this argmax recovery rather than on a directly
bundled label.

Edge case: very small cohorts degrade gracefully — singletons drop
the ribbon, cohorts of 2-4 drop the ribbon but keep individual
lines, cohorts of >=5 show the full thin-lines + mean + ribbon.
}
\examples{
panels <- plot_template_atlas("rna",
                                templates = "Early_Burst_Up")
length(panels)
names(panels)
}
