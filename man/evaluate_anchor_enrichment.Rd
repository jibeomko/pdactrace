% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit_score.R
\name{evaluate_anchor_enrichment}
\alias{evaluate_anchor_enrichment}
\alias{anchor_enrichment}
\title{Evaluate external-anchor enrichment in top-ranked genes}
\usage{
evaluate_anchor_enrichment(
  top_n = c(50, 100, 200, 500, 1000),
  tier = c("primary", "secondary", "exploratory", "all"),
  score_col = c("audit_score", "audit_score_median", "pareto_rank",
    "pareto_stability_top1", "tracd_confidence"),
  evidence = NULL,
  anchors = NULL
)

anchor_enrichment(...)
}
\arguments{
\item{top_n}{Integer vector of top-N cutoffs.}

\item{tier}{Anchor tier: \code{primary}, \code{secondary}, \code{exploratory}, or \code{all}.}

\item{score_col}{Ranking column. Defaults to deterministic \code{audit_score}.}

\item{evidence}{Optional user-supplied evidence table (see
\code{\link[=assemble_user_evidence]{assemble_user_evidence()}}). When \code{NULL} (default), the bundled
reference atlas is used.}

\item{anchors}{Optional user-supplied anchor table with columns
\code{gene}, \code{include_primary_eval}, and \code{include_secondary_eval}.
When \code{NULL} (default), the bundled \code{pdactrace_external_anchors}
table is used.}

\item{...}{Forwarded to \code{evaluate_anchor_enrichment()} when invoked
through the \code{anchor_enrichment()} short alias.}
}
\value{
A data.table with hypergeometric enrichment results.
}
\description{
External anchors are used only for evaluation. They are not used by the
frozen v0.3.0 scoring rule.
}
\examples{
evaluate_anchor_enrichment(top_n = 100, tier = "secondary")
}
