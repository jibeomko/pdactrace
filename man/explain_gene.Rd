% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explain_gene.R
\name{explain_gene}
\alias{explain_gene}
\title{Print a per-axis evidence summary for one gene}
\usage{
explain_gene(
  gene_symbol,
  view = c("evidence", "math", "both"),
  verbose = TRUE,
  reference = NULL
)
}
\arguments{
\item{gene_symbol}{HGNC gene symbol (length-1 character).}

\item{view}{One of \code{"evidence"} (default), \code{"math"}, or \code{"both"}.}

\item{verbose}{Logical. If \code{TRUE} (default), prints the formatted
sections to the console. The structured list is returned
invisibly either way.}

\item{reference}{Optional \code{data.table} to inject in place of the
bundled atlas (for tests / downstream pipelines).}
}
\value{
Invisibly, a list with elements \code{gene}, \code{view},
\code{provenance} (length-1 character or NA), and \code{math} (list-of-
axes from \code{\link[=evidence_math]{evidence_math()}} or NULL).
}
\description{
User-facing text formatter that wraps the two evidence layers
already in the package: \code{\link[=format_provenance]{format_provenance()}} (plain-English
phase-tag rollup) and \code{\link[=evidence_math]{evidence_math()}} (per-axis mathematical
values). Mirrors \code{\link[=explain_score]{explain_score()}} in pattern: prints to console
when \code{verbose = TRUE}, returns the underlying structured data
invisibly either way.
}
\details{
Three views are supported:

\describe{
\item{\code{"evidence"}}{Plain-English provenance summary only --
same content \code{\link[=format_provenance]{format_provenance()}} produces, plus the
gene's \code{audit_class} headline.}
\item{\code{"math"}}{Per-axis Evidence Math output -- eight axes
(trajectory_fit, effect_magnitude, cohort_consistency,
rna_protein_coupling, serum_bridge, cell_specificity,
filter_survival, clinical_role).}
\item{\code{"both"}}{Both, separated by a horizontal rule.}
}

Per-axis printing is the load-bearing design choice: it keeps
the output interpretable rather than hiding the evidence behind
a single composite score.
}
\examples{
explain_gene("LGALS3BP", view = "math", verbose = FALSE)
explain_gene("LTBP1", view = "evidence", verbose = FALSE)
explain_gene("LTBP1", view = "both", verbose = FALSE)
}
\seealso{
\code{\link[=evidence_math]{evidence_math()}}, \code{\link[=format_provenance]{format_provenance()}},
\code{\link[=explain_score]{explain_score()}}.
}
