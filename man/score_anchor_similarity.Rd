% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anchor_similarity.R
\name{score_anchor_similarity}
\alias{score_anchor_similarity}
\title{Descriptive feature-space similarity to bundled external anchors}
\usage{
score_anchor_similarity(
  genes = NULL,
  anchors = NULL,
  method = c("cosine", "euclidean"),
  tier = c("primary", "secondary", "all"),
  reference = NULL
)
}
\arguments{
\item{genes}{Optional character vector of gene symbols. \code{NULL}
(default) scores every gene in the reference.}

\item{anchors}{Optional \code{data.table} with columns \code{gene} and
\code{evidence_tier} (matching the bundled
\link{pdactrace_external_anchors} schema). \code{NULL} uses the bundled
anchor table.}

\item{method}{One of \code{"cosine"} (default) or \code{"euclidean"}.}

\item{tier}{Anchor tier filter: \code{"primary"} (T1_validated; the
smallest, highest-evidence set; default), \code{"secondary"}
(T1 + T2_literature_db), or \code{"all"} (also includes the single
exploratory anchor). Matches the partition used by
\code{\link[=evaluate_anchor_enrichment]{evaluate_anchor_enrichment()}}.}

\item{reference}{Optional \code{data.table} to inject in place of the
bundled atlas (for tests).}
}
\value{
A \code{data.table} with one row per requested gene and
columns \code{gene_symbol}, \code{anchor_similarity}, \code{anchor_n}
(number of anchor rows the centroid was built from), and
\code{anchor_tier}. Rows are sorted by \code{anchor_similarity}
descending with \code{NA} last.
}
\description{
Computes how close each gene's evidence-feature vector is to the
centroid of a curated positive-anchor reference set (cosine or
inverse-Euclidean similarity in the z-scored feature space). This
is the \strong{descriptive} layer that gives a continuous,
ML-flavoured prioritisation signal \strong{without training a
supervised classifier} -- the bundled anchor labels are used
only to define the centroid, never as training labels for a
shipped predictor.
}
\details{
This complements \code{\link[=evaluate_anchor_enrichment]{evaluate_anchor_enrichment()}}
(post-hoc top-N hypergeometric evaluation) and the deterministic
\code{audit_score}. The three are independent: \code{audit_score} ranks
by frozen rule, \code{anchor_similarity} ranks by feature-space
proximity to known biomarkers, and \code{evaluate_anchor_enrichment()}
is the evaluation harness that consumes either ranking via its
\code{score_col} argument.
}
\section{Method}{

\enumerate{
\item Build the per-gene feature matrix via
\code{\link[=make_evidence_features]{make_evidence_features()}} with \code{scale = "z"} (mean 0, sd 1
per feature over complete cases).
\item Resolve the anchor set via the same internal helper
\code{\link[=evaluate_anchor_enrichment]{evaluate_anchor_enrichment()}} uses, filtered by \code{tier}.
\item Anchor centroid \verb{c_k = mean over selected anchors of feature_k} (NAs skipped per feature).
\item For each atlas gene g, compute \verb{sim(g) = <z_g, c> / (||z_g|| * ||c||)} (cosine, default) or
\code{sim(g) = 1 / (1 + sqrt(sum_k (z_g_k - c_k)^2))} (Euclidean).
\item Genes with too few non-\code{NA} features (\verb{< 3}) get
\code{anchor_similarity = NA}.
}
}

\examples{
sim <- score_anchor_similarity(tier = "primary")
head(sim, 10)
}
\seealso{
\code{\link[=make_evidence_features]{make_evidence_features()}},
\code{\link[=evaluate_anchor_enrichment]{evaluate_anchor_enrichment()}}, \code{\link[=model_card]{model_card()}}.
}
