% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evidence_model.R
\name{predict_user_evidence_model}
\alias{predict_user_evidence_model}
\title{Predict from a \code{pdactrace_user_model}}
\usage{
predict_user_evidence_model(model, new_features, s = "lambda.min")
}
\arguments{
\item{model}{A \code{pdactrace_user_model} from
\code{\link[=fit_user_evidence_model]{fit_user_evidence_model()}}.}

\item{new_features}{\code{data.table} from \code{\link[=make_evidence_features]{make_evidence_features()}}
(or a frame with \code{gene_symbol} and the same numeric feature
columns the model was trained on).}

\item{s}{\code{lambda} value at which to evaluate. Default
\code{"lambda.min"}. Passed through to \code{predict.cv.glmnet()}.}
}
\value{
A \code{data.table} with \code{gene_symbol} and \code{predicted_prob}
(P(label=1) under the fitted model). Rows with any \code{NA}
feature are returned with \code{predicted_prob = NA}.
}
\description{
Predict from a \code{pdactrace_user_model}
}
\examples{
if (requireNamespace("glmnet", quietly = TRUE)) {
  set.seed(1)
  feats <- data.frame(gene_symbol = paste0("G", seq_len(30)),
                      x1 = rnorm(30), x2 = rnorm(30))
  y <- rep(c(0L, 1L), each = 15)
  fit <- fit_user_evidence_model(feats, y, nfolds = 3L)
  head(predict_user_evidence_model(fit, feats))
}

\donttest{
  feats <- make_evidence_features(scale = "z",
                                    drop_na_rows = TRUE)
  y <- as.integer(feats$gene_symbol \%in\%
                    c("LGALS3BP", "TIMP1", "LRG1"))
  fit <- fit_user_evidence_model(feats, y)
  head(predict_user_evidence_model(fit, feats))
}
}
