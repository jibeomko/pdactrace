% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify_trajectory.R
\name{classify_trajectory}
\alias{classify_trajectory}
\title{Classify each gene's trajectory against the 12-template catalog}
\usage{
classify_trajectory(fit, rho_cutoff = 0.85, sig_only = TRUE)
}
\arguments{
\item{fit}{A \code{data.table} returned by \code{\link[=fit_stage_de]{fit_stage_de()}}.}

\item{rho_cutoff}{Numeric. Minimum Pearson rho for assignment.
Default 0.85 (matches phase33 canonical).}

\item{sig_only}{Logical. If \code{TRUE} (default), restrict to
\code{lrt_significant == TRUE} rows.}
}
\value{
A \code{data.table} with the input plus columns:
\itemize{
\item \code{rna_pattern} - character: best 12-template match or NA. Note:
this returns the \emph{raw} best-match label (any of the 12 templates).
The atlas-surfaced version in \code{pdactrace_reference$rna_pattern}
is filtered to Early × 4 only; non-Early calls are flagged via
\code{excluded_mid_pattern} / \code{excluded_late_pattern} /
\code{excluded_monotonic_pattern}.
\item \code{rna_pattern_rho} - numeric: Pearson rho with best template
\item \code{rna_pattern_rho_runner_up} - second-best rho (gap detection)
}
}
\description{
Takes the output of \code{\link[=fit_stage_de]{fit_stage_de()}} and matches each LRT-significant
gene's z-scored 4-point profile (beta_N, beta_E, beta_M, beta_L) against
the \strong{canonical 12-template catalog} (Early × 4 + Mid × 4 + Late × 2 +
Monotonic × 2; v0.4.0). The best-match template is assigned if
rho >= \code{rho_cutoff}. The atlas surface (\code{rna_pattern} in
\code{pdactrace_reference}) restricts visible calls to \strong{Early × 4 only} —
Mid / Late / Monotonic best-matches are recorded internally but flagged
via \verb{excluded_*_pattern} columns rather than surfaced. Including the 8
non-Early templates in the \emph{matching} step is a deliberate design choice
for honest competition: a gene must beat 11 alternatives before it is
surfaced as Early.
}
\examples{
fit <- data.table::data.table(
  gene_symbol = c("G1", "G2"),
  beta_N = 0, beta_E = c(2, -2), beta_M = c(0.5, -0.5),
  beta_L = 0, lrt_padj = c(0.01, 0.02),
  lrt_significant = TRUE)
classify_trajectory(fit, sig_only = FALSE)

\donttest{
  fit <- fit_stage_de(my_counts, my_stage, my_cohort)
  pat <- classify_trajectory(fit, rho_cutoff = 0.85)
  table(pat$rna_pattern)
}
}
