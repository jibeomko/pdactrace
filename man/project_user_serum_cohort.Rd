% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project_user_serum_cohort.R
\name{project_user_serum_cohort}
\alias{project_user_serum_cohort}
\title{Project a user-supplied serum proteomics cohort (binary contrast)}
\usage{
project_user_serum_cohort(
  intensity,
  coldata,
  group_col = "group",
  pdac_label = "PDAC",
  hc_label = "HC",
  pan_label = NULL,
  test = c("limma_eBayes", "wilcox", "t_test"),
  padj_cutoff = 0.05,
  link_to_atlas = TRUE,
  reference = NULL
)
}
\arguments{
\item{intensity}{Log2 protein abundance matrix (genes x samples).}

\item{coldata}{\code{data.frame} of sample metadata.}

\item{group_col}{Name of the column in \code{coldata} carrying group
labels. Default \code{"group"}.}

\item{pdac_label}{Group label for PDAC samples. Default \code{"PDAC"}.}

\item{hc_label}{Group label for healthy-control samples.
Default \code{"HC"}.}

\item{pan_label}{Group label for pancreatitis samples (optional).
Default \code{NULL} (PDAC vs HC only; no Class A / B assignment
without pancreatitis context, but Class C and serum_detected
are still set).}

\item{test}{One of \code{"limma_eBayes"} (default; requires the
\code{limma} package), \code{"wilcox"}, or \code{"t_test"}.}

\item{padj_cutoff}{Significance threshold for \code{serum_detected}.
Default \code{0.05}. Genes with \code{padj_PDAC_vs_HC <= padj_cutoff}
are flagged as \code{serum_detected = TRUE}.}

\item{link_to_atlas}{Logical. If \code{TRUE} (default), join the
bundled \code{pdactrace_reference}'s \code{rna_pattern} direction to
assign \code{translation_class}. If \code{FALSE}, leave \code{translation_class}
as \code{NA} (defer to downstream code).}

\item{reference}{Optional \code{data.table} to inject in place of the
bundled atlas (used by tests).}
}
\value{
A \code{data.table} with one row per gene in \code{intensity} and
columns:
\itemize{
\item \code{gene_symbol}
\item \code{serum_log2fc_PDAC_vs_HC}, \code{serum_padj_PDAC_vs_HC}
\item \code{serum_log2fc_Pan_vs_HC}, \code{serum_padj_Pan_vs_HC}
(NA if \code{pan_label = NULL})
\item \code{translation_class} (\code{"A"} / \code{"B"} / \code{"C"} / \code{NA})
\item \code{serum_detected} (logical)
}
Suitable for direct use as \code{assemble_user_evidence( serum_summary = ...)}.
}
\description{
The serum side of the framework only requires \strong{binary group
contrasts} -- PDAC vs healthy control (HC) and optionally
Pancreatitis vs HC -- not stage-aware data. This makes user
projection asymmetric to the tissue side: where
\code{\link[=project_user_cohort]{project_user_cohort()}} needs Normal / Early / Mid / Late stage
labels for its 12-template trajectory matching, this helper
accepts the kind of serum DIA dataset most public PDAC serum
studies actually publish (PDAC + HC ± pancreatitis, no clinical
stage labels needed).
}
\section{What this function does}{

\enumerate{
\item Splits samples into PDAC / HC / (optional) Pancreatitis
groups by \code{coldata[[group_col]]}.
\item Computes \code{serum_log2fc_PDAC_vs_HC = mean(PDAC) - mean(HC)}
and (if \code{pan_label} is set)
\code{serum_log2fc_Pan_vs_HC = mean(Pan) - mean(HC)} per gene.
\item Runs a per-gene two-group test (default \code{limma_eBayes};
fallback to \code{wilcox} or \code{t_test}) to attach BH-adjusted
p-values for each contrast.
\item Optionally joins the bundled
\link{pdactrace_reference}'s \code{rna_pattern} direction to derive a
per-gene \code{translation_class} (A / B / C):
\itemize{
\item \strong{Class A} -- tissue and serum agree on direction
('same dir.', concordant translation).
\item \strong{Class B} -- tissue and serum oppose
('opposite', inverse translation; the rare,
pancreatitis-discriminating case).
\item \strong{Class C} -- decoupled (tissue direction unclear,
or serum log2FC very small).
}
Set \code{link_to_atlas = FALSE} to skip the atlas join and emit
\code{translation_class = NA} (deferring class assignment to
downstream code).
}

Output is shaped to plug straight into
\code{\link[=assemble_user_evidence]{assemble_user_evidence()}} via the \code{serum_summary} argument.
}

\section{Input format}{

\describe{
\item{\code{intensity}}{A numeric matrix or \code{data.frame} of
\strong{log2 protein abundance}, with \strong{genes (HGNC symbols) in
rows} and \strong{samples in columns}. Pre-log2 transformation
is required; raw MS intensities will produce nonsensical
log2FC values.}
\item{\code{coldata}}{A \code{data.frame} with at least one column
(\code{group_col}) carrying group labels matching \code{pdac_label},
\code{hc_label}, and (optionally) \code{pan_label}. \code{nrow(coldata)}
must equal \code{ncol(intensity)}. Sample order must match
column order of \code{intensity}.}
}
Minimum 3 samples per group is recommended (\code{limma} and
\code{wilcox} need at least that for a stable variance estimate /
rank distribution). Fewer samples emit a \code{warning}.
}

\examples{
\dontrun{
  # Synthetic example: 50 genes x 24 samples (8 PDAC, 8 HC,
  # 8 Pancreatitis) with PDAC-up + Pan-flat signal.
  set.seed(1)
  g <- 50; n <- 24
  intensity <- matrix(rnorm(g * n, mean = 5),
                      nrow = g, ncol = n,
                      dimnames = list(paste0("GENE", seq_len(g)),
                                       paste0("S", seq_len(n))))
  intensity[1:10, 1:8] <- intensity[1:10, 1:8] + 2  # PDAC-up
  coldata <- data.frame(
    sample = colnames(intensity),
    group  = rep(c("PDAC", "HC", "Pancreatitis"), each = 8))

  serum_summary <- project_user_serum_cohort(
    intensity, coldata,
    group_col  = "group",
    pdac_label = "PDAC",
    hc_label   = "HC",
    pan_label  = "Pancreatitis")
  head(serum_summary)

  # Feed into the downstream evidence assembler:
  ev <- assemble_user_evidence(serum_summary = serum_summary)
}
}
\seealso{
\code{\link[=project_user_cohort]{project_user_cohort()}} for the \strong{tissue} side
(stage-aware), \code{\link[=assemble_user_evidence]{assemble_user_evidence()}} for combining
tissue + serum into the audit-rule input,
\code{\link[=plot_serum_direction]{plot_serum_direction()}} for visualisation.
}
