% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trace_filters.R
\name{trace_filters}
\alias{trace_filters}
\title{Trace which selection filters a gene passed or failed}
\usage{
trace_filters(gene)
}
\arguments{
\item{gene}{Character vector of HGNC symbols.}
}
\value{
A \code{data.table} with one row per requested gene and one
column per filter step (logical). Includes \code{n_phase60_pass}
(0-7) and \code{class_route} summary column.
}
\description{
Returns the gene's pass/fail status for each step of pdactrace's
two parallel narrowing funnels:
}
\details{
\itemize{
\item \strong{\code{phase60} 7-step pipeline} - strict cumulative funnel
(signal peptide -> serum measurable -> pool significance -> vs
pancreatitis -> HC-in-middle -> direction match -> final).
\item \strong{\code{phase77} strict candidate set} - independent narrow funnel
producing the 22 strict tissue+serum convergent candidates
that anchor manual Class B curation.
}

Most genes (including LTBP1) pass \emph{one} of the two funnels but not
both - this is the audit-trail message of pdactrace.
}
\examples{
  trace_filters("LTBP1")
  trace_filters(c("LTBP1", "SPARC", "CDH13", "SERPINA1"))
}
