% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify_protein_trajectory.R
\name{classify_protein_trajectory}
\alias{classify_protein_trajectory}
\alias{classify_prot_trajectory}
\title{Classify each protein's trajectory against the 12-template catalog}
\usage{
classify_protein_trajectory(fit, rho_cutoff = 0.85, sig_only = TRUE)

classify_prot_trajectory(...)
}
\arguments{
\item{fit}{A \code{data.table} returned by \code{\link[=fit_stage_de_protein]{fit_stage_de_protein()}}.}

\item{rho_cutoff}{Numeric. Minimum Pearson rho for assignment.
Default 0.85 (matches phase34 canonical).}

\item{sig_only}{Logical. If \code{TRUE} (default), restrict to
\code{lrt_significant == TRUE} rows.}

\item{...}{Forwarded to \code{classify_protein_trajectory()} when invoked
through the \code{classify_prot_trajectory()} short alias.}
}
\value{
A \code{data.table} with columns \code{gene_symbol}, \code{prot_pattern},
\code{prot_pattern_rho}, \code{prot_pattern_rho_runner_up}. The remaining
columns of the input fit are preserved.
}
\description{
Protein-side wrapper of \code{\link[=classify_trajectory]{classify_trajectory()}}: takes the output of
\code{\link[=fit_stage_de_protein]{fit_stage_de_protein()}}, runs the same shared template-matching
engine (12 templates: Early x 4 + Mid x 4 + Late x 2 + Monotonic x 2),
and renames the output column \code{rna_pattern} to \code{prot_pattern} so
\code{\link[=assemble_user_evidence]{assemble_user_evidence()}} can consume both layers without colliding
on column names. As with the RNA side, the atlas surface restricts
visible \code{prot_pattern} calls to Early x 4; non-Early best-matches are
recorded internally but not surfaced.
}
\examples{
prot_fit <- data.table::data.table(
  gene_symbol = c("P1", "P2"),
  beta_N = 0, beta_E = c(2, -2), beta_M = c(0.5, -0.5),
  beta_L = 0, lrt_padj = c(0.01, 0.02),
  lrt_significant = TRUE)
classify_protein_trajectory(prot_fit, sig_only = FALSE)

\donttest{
  prot_fit <- fit_stage_de_protein(my_intensity, my_stage, my_cohort)
  prot_pat <- classify_protein_trajectory(prot_fit)
}
}
