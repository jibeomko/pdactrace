% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_summarized_experiment.R
\name{as_summarized_experiment}
\alias{as_summarized_experiment}
\title{Convert pdactrace_reference into a SummarizedExperiment}
\usage{
as_summarized_experiment(reference = NULL)
}
\arguments{
\item{reference}{Optional override of the bundled
\code{pdactrace_reference}. Default \code{NULL} uses the bundled atlas.
Useful for unit tests on a synthetic atlas-shaped table.}
}
\value{
A \code{SummarizedExperiment} with:
\itemize{
\item 2 assays — \code{rna_beta} (10,113 genes by 4 stages) and
\code{rna_lfcSE} (same shape; \code{NA} in the \code{Normal} column).
\item \code{colData} — 4 rows: \code{stage = c("Normal", "Early", "Mid", "Late")},
\code{reference_level = c(TRUE, FALSE, FALSE, FALSE)}.
\item \code{rowData} — every non-stage-axis column of the bundled atlas
(~109 columns: identifiers, audit components, trajectory
pattern calls, meta-analysis summaries, etc.).
\item \code{metadata} — \code{list(atlas_version, build_date, n_cohorts, ...)}.
}
}
\description{
Returns a \code{SummarizedExperiment} view of the bundled v0.4.0 atlas
in which the four stage-effect coefficient columns
(\code{rna_beta_N}, \code{rna_beta_E}, \code{rna_beta_M}, \code{rna_beta_L}) become a
4-column \code{assay}, the standard errors \code{rna_lfcSE_E/M/L} (with \code{NA}
for the \code{Normal} reference column) become a parallel \code{assay}, and
all of the per-gene metadata (audit components, RNA / protein
trajectory pattern, translation class, scRNA cell origin, serum
direction, meta-analysis I2, etc.) goes into \code{rowData()}. The
\code{colData()} carries one row per stage with a \code{reference_level}
flag for the Normal column.
}
\details{
Intended use: Bioconductor-native consumption of the atlas with
\code{assay()}, \code{rowData()}, \code{subsetByOverlaps()} (when paired with
a separate ranges object), and downstream tooling that expects
the SE container — without forcing a one-time mass conversion of
the bundled object (which remains a \code{data.table} for fast
query-based use cases).
}
\examples{
\dontrun{
  if (requireNamespace("SummarizedExperiment", quietly = TRUE)) {
    se <- as_summarized_experiment()
    se
    SummarizedExperiment::assay(se, "rna_beta")[1:3, ]
    head(SummarizedExperiment::rowData(se))
  }
}
}
