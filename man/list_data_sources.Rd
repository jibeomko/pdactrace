% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list_data_sources.R
\name{list_data_sources}
\alias{list_data_sources}
\title{List the public datasets that contributed to the pdactrace atlas}
\usage{
list_data_sources(layer = NULL)
}
\arguments{
\item{layer}{Optional character vector to filter by data layer:
one or more of \code{"RNA"}, \code{"Protein"}, \code{"scRNA"}, \code{"Serum"},
\code{"Pancreatitis"}, \code{"Validation"}. Default \code{NULL} returns the
full table.}
}
\value{
A \code{data.table} with columns:
\itemize{
\item \code{accession}   — public identifier (GEO ID, PXD, etc.)
\item \code{layer}       — RNA / Protein / scRNA / Serum / ...
\item \code{source_type} — repository name (GEO / PRIDE / PDC / ...)
\item \code{used_for}    — short role description
\item \code{citation}    — primary publication citation when available
\item \code{url}         — direct repository URL
}
}
\description{
Returns the bundled \code{pdactrace_data_sources} table — one row per
public dataset (GEO / PRIDE / PDC / ICGC / GTEx / MassIVE / SRA)
that was used to populate the v0.99.x reference atlas. Designed so
a Bioconductor reviewer or end user can answer "where does this
data come from?" programmatically rather than by chasing prose in
the manuscript.
}
\examples{
head(list_data_sources())
list_data_sources(layer = "Serum")
nrow(list_data_sources())
}
