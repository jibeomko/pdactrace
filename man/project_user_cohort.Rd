% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project_user_cohort.R
\name{project_user_cohort}
\alias{project_user_cohort}
\title{End-to-end user-cohort projection through the audit framework}
\usage{
project_user_cohort(
  rna,
  coldata = NULL,
  stage_col,
  cohort_col = NULL,
  protein = NULL,
  protein_assay_name = "intensity",
  signal_peptide = NULL,
  sig_only = FALSE
)
}
\arguments{
\item{rna}{Either an integer count matrix / data.frame (genes by
samples) or a \code{SummarizedExperiment}. Required.}

\item{coldata}{Optional \code{data.frame} / \code{DataFrame} of sample-level
metadata. Required when \code{rna} is a matrix; ignored when \code{rna} is
a \code{SummarizedExperiment} (its \code{colData()} is used instead).}

\item{stage_col}{Name of the column in \code{coldata} (or
\code{colData(rna)}) that carries the stage labels.}

\item{cohort_col}{Optional name of the cohort column. Default
\code{NULL} (single-cohort).}

\item{protein}{Optional second \code{SummarizedExperiment} or numeric
intensity matrix carrying log2 protein intensities for the same
stage / cohort design. When supplied, the protein side runs
through \code{\link[=fit_stage_de_protein]{fit_stage_de_protein()}} +
\code{\link[=classify_protein_trajectory]{classify_protein_trajectory()}} and is layered into
\code{assemble_user_evidence(rna_fit, prot_fit)}. Default \code{NULL}.}

\item{protein_assay_name}{Assay slot to use when \code{protein} is a
\code{SummarizedExperiment}. Default \code{"intensity"}.}

\item{signal_peptide}{Optional character vector of gene symbols
carrying a signal peptide; passed straight to
\code{\link[=assemble_user_evidence]{assemble_user_evidence()}}.}

\item{sig_only}{Passed to \code{\link[=classify_protein_trajectory]{classify_protein_trajectory()}} (default
\code{FALSE} here so small / noisy cohorts still surface
best-template calls).}
}
\value{
A \code{list} of class \code{pdactrace_user_projection} with
elements:
\itemize{
\item \code{rna_fit}     -- output of \code{fit_stage_de()}.
\item \code{rna_pattern} -- output of \code{classify_trajectory()}.
\item \code{prot_fit}    -- output of \code{fit_stage_de_protein()} (or \code{NULL}).
\item \code{prot_pattern}-- output of \code{classify_protein_trajectory()}
(or \code{NULL}).
\item \code{evidence}    -- output of \code{assemble_user_evidence()}.
\item \code{audit}       -- output of \code{compute_audit_score(evidence = ...)}.
\item \code{summary}     -- one-row \code{data.table} with input shape +
audit-class counts.
}
}
\description{
One-call wrapper that takes a user RNA-seq cohort (and optionally a
tissue-protein cohort) and walks it end-to-end through the four
v0.99 framework steps: stage-aware DE fit, 12-template trajectory
matching, evidence-table assembly, and frozen audit scoring.
Returns a structured list with each intermediate object plus the
final audit table, so the user can inspect at any step.
}
\details{
This is a convenience around the chain
\code{\link[=fit_stage_de]{fit_stage_de()}} -> \code{\link[=classify_trajectory]{classify_trajectory()}} ->
\code{\link[=assemble_user_evidence]{assemble_user_evidence()}} -> \code{\link[=compute_audit_score]{compute_audit_score()}}.
Calling those four explicitly continues to work; this wrapper
reduces a typical user-cohort workflow to a single line and
provides a consistent return shape for reporting and review.

Bioconductor-native input is supported via the same S4 method
dispatch as \code{fit_stage_de()}: the \code{rna} argument can be either a
count matrix / data.frame plus \code{coldata}, or a
\code{SummarizedExperiment} whose \code{assay()} is the count matrix and
whose \code{colData()} carries the stage / cohort columns.
}
\examples{
data(toy_counts)
data(toy_coldata)
if (requireNamespace("DESeq2", quietly = TRUE)) {
  res <- project_user_cohort(
    rna = toy_counts, coldata = toy_coldata,
    stage_col = "stage", cohort_col = "cohort")
  res$summary
}

\donttest{
  if (requireNamespace("DESeq2", quietly = TRUE)) {
    res <- project_user_cohort(
      rna        = toy_counts,
      coldata    = toy_coldata,
      stage_col  = "stage",
      cohort_col = "cohort",
      protein    = toy_protein)
    res$summary
    head(res$audit[order(-audit_score)])
  }
}
}
