% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pareto_audit.R
\name{compute_pareto_layers}
\alias{compute_pareto_layers}
\title{Compute weight-free Pareto layers on the three evidence axes}
\usage{
compute_pareto_layers(
  atlas = NULL,
  axes = c("audit_evidence_strength", "audit_biological_coherence",
    "audit_translational_relevance"),
  gate_cols = c("audit_leakage_gate", "audit_heterogeneity_gate"),
  score_col = "audit_score",
  gate_filter = TRUE,
  top_n = 2000L
)
}
\arguments{
\item{atlas}{Optional user-supplied atlas (defaults to the bundled
\code{pdactrace_reference}). When non-NULL it must contain the columns
referenced by \code{axes}, \code{gate_cols}, and \code{score_col}.}

\item{axes}{Character(3) of the axis column names. Defaults to the
three frozen axes.}

\item{gate_cols}{Character(2) of the gate column names. Defaults to
\code{c("audit_leakage_gate", "audit_heterogeneity_gate")}.}

\item{score_col}{Column used both to restrict the candidate pool to
the top \code{top_n} rows and as a deterministic tiebreaker after
crowding distance. Defaults to \code{"audit_score"}.}

\item{gate_filter}{Logical; when \code{TRUE} (default), genes with any
gate value \verb{< 1} are excluded from the Pareto candidate pool and
recorded with \code{pareto_excluded_by_gate = TRUE}.}

\item{top_n}{Integer; restrict the Pareto candidate pool to the top
\code{top_n} rows by \code{score_col}. Defaults to \code{2000L} which comfortably
covers the BiB headline top-100 anchor enrichment.}
}
\value{
A data.table with one row per gene in \code{atlas} and the
columns \code{pareto_layer} (integer; \code{NA} for excluded genes),
\code{crowding_distance} (numeric; \code{Inf} for boundary points,
\code{NA} for excluded), \code{pareto_rank} (integer 1..N within the
candidate pool, \code{NA} for excluded), and \code{pareto_excluded_by_gate}
(logical).
}
\description{
\code{compute_pareto_layers()} implements layered non-dominated sorting on
\code{audit_evidence_strength}, \code{audit_biological_coherence}, and
\code{audit_translational_relevance}. The two reliability gates
(\code{audit_leakage_gate}, \code{audit_heterogeneity_gate}) are applied as a
hard pre-filter rather than as weights, mirroring the
"axes graded, gates reliability filter" structure of the frozen 3+2
audit framework. Ranking is layer-first, with crowding distance
(NSGA-II convention) as a within-layer tiebreaker and the frozen
deterministic \code{audit_score} as a final deterministic tiebreaker.
}
\details{
This function is complementary to \code{\link[=compute_audit_score]{compute_audit_score()}} and does
not modify the frozen weighted formula. Use it when reviewers ask
whether the audit ranking is driven by the scalar weights
0.40 / 0.35 / 0.25 — Pareto layers expose a weight-free alternative.
}
\examples{
head(compute_pareto_layers(top_n = 200L))
}
