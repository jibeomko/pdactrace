% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_gene_template.R
\name{plot_gene_template}
\alias{plot_gene_template}
\title{Per-gene template-overlay plot}
\usage{
plot_gene_template(
  gene_symbol,
  layer = c("rna", "protein"),
  highlight_color = "#C62828",
  output_file = NULL,
  width = 1.55,
  height = 1.4,
  reference = NULL
)
}
\arguments{
\item{gene_symbol}{HGNC gene symbol (length-1 character).}

\item{layer}{Either \code{"rna"} (default) or \code{"protein"}.}

\item{highlight_color}{Colour of the gene-highlight line. Default
\code{"#C62828"} (NCS-grade muted red).}

\item{output_file}{Optional output PDF path. If \code{NULL} (default),
the function returns a \code{ggplot} object; otherwise it writes one
cairo PDF via \code{\link[=pdactrace_save]{pdactrace_save()}} and returns the path invisibly.}

\item{width, height}{PDF size in inches when \code{output_file} is set.
Defaults to the compact 1.55 x 1.40 fig2C style.}

\item{reference}{Optional override of the bundled
\code{pdactrace_reference} (RNA layer) for unit-test injection.}
}
\value{
Either a \code{ggplot} object (when \code{output_file} is \code{NULL}) or
the absolute path of the written PDF (invisibly).
}
\description{
For one gene/protein, draws the trajectory-template panel that
the gene was matched to (12-template Pearson-rho argmax) with the
gene's own z-scored Normal/Early/Mid/Late trajectory overlaid as
a highlight line. Companion to the atlas-wide
\code{\link[=plot_template_atlas]{plot_template_atlas()}}: that one walks all 12 templates without
a gene; this one zeroes in on a single gene's matched panel.
}
\details{
The matched template is recovered via the same Pearson-rho argmax
across the 12 bundled templates that backs \code{\link[=classify_trajectory]{classify_trajectory()}}
and \code{\link[=score_trajectory]{score_trajectory()}}. The matched rho is annotated as a small
caption.
}
\examples{
p <- plot_gene_template("LGALS3BP", "rna")
class(p)
p$labels$title
}
