% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explain_score.R
\name{explain_score}
\alias{explain_score}
\title{Decompose a gene's audit score into its three axes and two gates}
\usage{
explain_score(gene_symbol, verbose = TRUE)
}
\arguments{
\item{gene_symbol}{HGNC gene symbol (length-1 character).}

\item{verbose}{Logical. If \code{TRUE} (default), prints a one-paragraph
plain-language summary to the console. The structured breakdown
is returned invisibly either way.}
}
\value{
Invisibly returns a list with elements:
\itemize{
\item \code{gene} — the resolved HGNC symbol.
\item \code{audit_class}, \code{audit_score}, \code{positive_score} — top-line scalars.
\item \code{axes} — \code{data.table} (3 rows): name, weight, value, contribution.
\item \code{gates} — \code{data.table} (2 rows): name, value, triggered_by.
\item \code{explanation} — character(1), one-paragraph rationale.
}
}
\description{
Returns a transparent breakdown of how the frozen v0.3.0 audit rule
arrived at one gene's \code{audit_score} and \code{audit_class}. The point is
to make the score auditable in plain English: \emph{"LTBP1 lands in
\code{supported_uncertain} because its \code{heterogeneity_gate} = 0.7 —
the cohort-level meta-analysis I2 exceeds 70\% on at least one
contrast."}
}
\details{
The audit rule is a frozen weighted sum:

\if{html}{\out{<div class="sourceCode text">}}\preformatted{positive_score = 0.40 * evidence_strength
               + 0.35 * biological_coherence
               + 0.25 * translational_relevance
audit_score    = positive_score * leakage_gate * heterogeneity_gate
}\if{html}{\out{</div>}}

This function reports each component, the gate state, the audit
label that the gate transitions trigger, and a short prose
explanation derived from those numbers. It does NOT recompute any
new evidence — it reads the bundled atlas and presents what the
frozen rule already produced. Verbose dispatch is intended for
interactive console use; the underlying \code{data.table} is also
returned for programmatic use.
}
\examples{
explain_score("LGALS3BP")
explain_score("LTBP1")
explain_score("GAPDH")
}
