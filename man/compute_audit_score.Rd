% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit_score.R
\name{compute_audit_score}
\alias{compute_audit_score}
\title{Compute the frozen pdactrace audit score (3-axis + 2-gate)}
\usage{
compute_audit_score(gene_symbol = NULL, evidence = NULL)
}
\arguments{
\item{gene_symbol}{Optional character vector of HGNC gene symbols.
When \code{NULL} (default) returns the full atlas.}

\item{evidence}{Optional user-supplied evidence table (e.g.,
produced by \code{\link[=assemble_user_evidence]{assemble_user_evidence()}}) to score in place of the
bundled atlas. When \code{NULL} (default), the frozen v0.3.0 reference
atlas is used; the framework discipline (frozen weights, gates)
applies identically to either input.}
}
\value{
A data.table with \code{evidence_strength},
\code{biological_coherence}, \code{translational_relevance},
\code{leakage_gate}, \code{heterogeneity_gate}, \code{positive_score},
\code{audit_score}, and \code{audit_class}.
}
\description{
Aggregates the seven implementation features into three transparent
evidence axes (\code{evidence_strength}, \code{biological_coherence},
\code{translational_relevance}) and applies two pre-specified reliability
gates (\code{leakage_gate}, \code{heterogeneity_gate}) before computing
\code{audit_score = positive_score * leakage_gate * heterogeneity_gate}.
Each gene receives one of four \code{audit_class} labels:
\code{high_confidence}, \code{supported_uncertain}, \code{penalized}, or \code{excluded}.
}
\details{
This is not a supervised biomarker predictor; the formula is
pre-specified and external anchors are used only post-freeze for
enrichment evaluation. The seven implementation features are kept as
supplementary detail; see \code{extract_graph_features()}.
}
\examples{
compute_audit_score(c("LGALS3BP", "LTBP1", "ALB", "GAPDH"))
}
