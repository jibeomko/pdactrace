% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_gene_hexagon.R
\name{plot_gene_hexagon}
\alias{plot_gene_hexagon}
\title{Hexagonal evidence radar for one or more genes}
\usage{
plot_gene_hexagon(
  gene_symbol,
  comparison = NULL,
  show_score = TRUE,
  palette = NULL
)
}
\arguments{
\item{gene_symbol}{Character vector of one or more HGNC gene
symbols. Multiple genes are overlaid as separate polygons.}

\item{comparison}{Optional. Either \code{NULL} (default), a single
gene symbol, or one of \code{"high_confidence_mean"},
\code{"supported_uncertain_mean"}, \code{"penalized_mean"},
\code{"excluded_mean"}. Adds a translucent reference polygon for
visual context.}

\item{show_score}{Logical. If \code{TRUE} (default), annotate the
centre with each gene's \code{audit_score} and \code{audit_class}.}

\item{palette}{Optional named character vector of polygon colors
keyed by gene symbol. Defaults to the pdactrace audit-class
palette where possible, otherwise to a Set1-style palette.}
}
\value{
A \code{ggplot2} object (NCS-grade compact, cairo-pdf safe).
}
\description{
Draws a compact 6-axis hexagonal radar chart summarising the
pdactrace audit-feature components for one or more genes. Inspired
by the multi-polygon radar style used in graph-of-graphs analyses
(e.g., GATE; Liu and Cheng 2025), but reuses the
six interpretable feature axes from \code{extract_graph_features()}
rather than learned graph embeddings.
}
\details{
The six axes (clockwise from the top vertex) are:
\enumerate{
\item \strong{Multi-layer} — \code{score_layer} (fraction of RNA/Protein/scRNA/Serum
layers with a measurable signal).
\item \strong{Direction} — \code{score_direction} (cross-cohort + cross-layer
direction concordance).
\item \strong{Stage-onset} — \code{score_early} (Pearson rho against the four
Early templates, weighted by LRT significance).
\item \strong{Serum bridge} — \code{score_serum} (serum detected + signal
peptide + tissue-to-serum direction concordance).
\item \strong{Leakage safety} — \code{leakage_mult} (1 if clean, 0.5 if plasma
high-abundance, 0 if housekeeping).
\item \strong{Cohort consistency} — \code{het_mult} (1 if I² < 70, 0.7 if 70-90,
0.3 if >= 90).
}

Each axis is plotted on a fixed 0--1 scale; concentric hexagons
mark the 0.25 / 0.50 / 0.75 / 1.00 contours.
}
\examples{
plot_gene_hexagon("LTBP1")
plot_gene_hexagon(c("LGALS3BP", "LTBP1"))
plot_gene_hexagon("LTBP1", comparison = "high_confidence_mean")
}
