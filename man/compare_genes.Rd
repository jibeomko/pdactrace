% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_genes.R
\name{compare_genes}
\alias{compare_genes}
\title{Side-by-side Evidence Math comparison across multiple genes}
\usage{
compare_genes(gene_symbols, axes = NULL, wide = FALSE, reference = NULL)
}
\arguments{
\item{gene_symbols}{Character vector of HGNC gene symbols.}

\item{axes}{Optional character vector to filter the axis set
reported. Defaults to all axes from \code{\link[=evidence_math]{evidence_math()}}.
Recognised values: \code{"trajectory_fit"}, \code{"effect_magnitude"},
\code{"cohort_consistency"}, \code{"rna_protein_coupling"},
\code{"serum_bridge"}, \code{"cell_specificity"},
\code{"filter_survival"}, \code{"clinical_role"}.}

\item{wide}{Logical. If \code{TRUE}, returns one row per gene with
\code{axis.metric} columns. Default \code{FALSE} (long form).}

\item{reference}{Optional \code{data.table} to inject in place of
the bundled atlas (used by tests).}
}
\value{
A \code{data.table}. Long form columns: \code{gene}, \code{axis},
\code{metric}, \code{value} (character; numerics formatted to 4 sig
figs to keep the table renderable). Wide form: \code{gene}
followed by \code{axis.metric} columns.
}
\description{
Calls \code{\link[=evidence_math]{evidence_math()}} for each input gene and pivots the
per-axis lists into a tidy \code{data.table}. Useful for assembling
the "candidate vs. competitors" comparison tables that show up
in the manuscript and in \code{report_gene()} output.
}
\details{
By default returns a long table with one row per
(gene x axis x metric) tuple — friendly to \code{data.table::dcast()}
if you need a custom wide layout. Set \code{wide = TRUE} for the
convenience pivot to one row per gene with metric columns
(\code{axis.metric}, e.g. \code{trajectory_fit.delta_rho}).

Note: this function reports the \emph{math evidence} per axis. For
the audit-score-driven candidate ranking with redundancy
grouping, see \code{\link[=compare_candidates]{compare_candidates()}} (different layer, same
atlas).
}
\examples{
compare_genes(c("LGALS3BP", "LTBP1", "TIMP1"))
compare_genes(c("LGALS3BP", "LTBP1"),
              axes = c("trajectory_fit", "rna_protein_coupling"),
              wide = TRUE)
}
\seealso{
\code{\link[=evidence_math]{evidence_math()}}, \code{\link[=explain_gene]{explain_gene()}},
\code{\link[=compare_candidates]{compare_candidates()}}.
}
