% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_candidates.R
\name{compare_candidates}
\alias{compare_candidates}
\title{Compare multiple candidate genes side-by-side}
\usage{
compare_candidates(gene_symbols)
}
\arguments{
\item{gene_symbols}{Character vector of HGNC gene symbols
(length >= 2; length-1 input is allowed but the comparison is
trivial).}
}
\value{
A \code{data.table} (one row per gene) with columns:
\itemize{
\item \code{gene_symbol}, \code{audit_class}, \code{audit_score}
\item \code{rna_pattern}, \code{prot_pattern}, \code{translation_class}
\item \code{cell_origin_top}, \code{serum_detected},
\code{serum_log2fc_PDAC_vs_HC}
\item \code{max_I2_meta}, \code{redundancy_with} — the latter lists other genes
in the input set that share \code{rna_pattern} \emph{and} \code{cell_origin_top}
(semicolon-separated, "" if none).
}
}
\description{
Returns a single \code{data.table} row-per-gene with the columns most
useful for panel-design discussion: audit class + score, RNA and
protein trajectory pattern, translation class, dominant scRNA cell
origin, serum-detectability flags, and a redundancy hint based on
shared cell origin and shared RNA pattern. The result is sorted by
\code{audit_score} (descending) so the strongest candidates surface
first.
}
\details{
Input genes that are not in the bundled atlas are returned with
\code{audit_class = NA}, so the call never fails on stray symbols — the
returned table makes the gap explicit.
}
\examples{
compare_candidates(c("LGALS3BP", "LTBP1", "SERPINA1", "ALB", "GAPDH"))
}
