% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/claim_audit.R
\name{classify_claim_tier}
\alias{classify_claim_tier}
\title{Classify tissue-to-blood biomarker claim tiers}
\usage{
classify_claim_tier(
  atlas = NULL,
  genes = NULL,
  tau_tissue = 0.5,
  tau_serum = 0.1
)
}
\arguments{
\item{atlas}{Optional atlas-like data table. Defaults to the bundled
\code{pdactrace_reference}.}

\item{genes}{Optional character vector of genes to return. Ranking and
thresholds are computed on the supplied atlas, then filtered.}

\item{tau_tissue}{Minimum absolute tissue effect used by
\code{\link[=compute_trace_d]{compute_trace_d()}}.}

\item{tau_serum}{Minimum absolute serum log2 fold-change used by
\code{\link[=compute_trace_d]{compute_trace_d()}} and the serum-signal flag.}
}
\value{
A data.table with one row per gene and claim audit columns.
}
\description{
\code{classify_claim_tier()} separates evidence strength from claim
strength. A gene can have strong stage-aware tissue evidence while
still receiving a conservative blood-biomarker claim such as
\code{translation_unknown} or \code{confounded}.
}
\examples{
classify_claim_tier(genes = c("LGALS3BP", "LTBP1", "ALB", "GAPDH"))
}
