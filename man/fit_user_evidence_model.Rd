% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evidence_model.R
\name{fit_user_evidence_model}
\alias{fit_user_evidence_model}
\title{Fit a user-supplied supervised model on the evidence feature matrix}
\usage{
fit_user_evidence_model(
  features,
  labels,
  method = "elastic_net",
  alpha = 0.5,
  nfolds = 10L,
  weights = NULL,
  seed = 1L
)
}
\arguments{
\item{features}{\code{data.table} from \code{\link[=make_evidence_features]{make_evidence_features()}} (or
any frame with \code{gene_symbol} plus numeric feature columns).
Z-scored input (\code{scale = "z"}) is recommended for stable
coefficients.}

\item{labels}{Integer / logical vector of length \code{nrow(features)},
one entry per row, with \code{1} / \code{TRUE} for positives and
\code{0} / \code{FALSE} for negatives.}

\item{method}{Currently only \code{"elastic_net"} is implemented.
Argument exists so a future release can add \code{"rf"} /
\code{"gbm"} without API churn.}

\item{alpha}{Elastic-net mixing parameter in \verb{[0, 1]}. \code{0} is
ridge, \code{1} is lasso. Default \code{0.5} (mid-point) keeps non-zero
coefficients while damping correlated features.}

\item{nfolds}{Number of CV folds. Default \code{10}.}

\item{weights}{Optional per-row observation weights passed
through to \code{glmnet::cv.glmnet()}. Default \code{NULL}
(uniform weights).}

\item{seed}{Integer RNG seed for reproducibility. Default \code{1L}.}
}
\value{
An S3 object of class \code{pdactrace_user_model} (a list)
with elements: \code{cvfit} (the underlying \code{cv.glmnet} object),
\code{coef_table} (data.table: \code{feature}, \code{coef_value}),
\code{lambda_min}, \code{lambda_1se}, \code{cv_auc_mean}, \code{cv_auc_sd},
\code{n_train}, \code{n_features}, \code{n_positives}, \code{top_pos},
\code{top_neg}, \code{alpha}, \code{seed}, \code{method}, \code{feature_set_version},
\code{card}.
}
\description{
Wraps \code{glmnet::cv.glmnet(family = "binomial")} to fit a sparse
logistic model that ranks genes by feature-space evidence. The
fit is \strong{owned entirely by the user}: labels must be supplied
explicitly. The package ships no pretrained model -- this is a
deliberate constraint for PDAC biomarker discovery, where
validated non-circular ground truth is unavailable.
}
\details{
Use case: a user has their own positive set (e.g. their lab's
validated panel) and wants a continuous prioritisation score
across the rest of the atlas, with feature-level
interpretability that survives Bioconductor review.
}
\section{Reproducibility}{

RNG is scoped via \code{\link[withr:with_seed]{withr::local_seed()}} inside the fit. Two
calls with the same \verb{(features, labels, alpha, nfolds, seed)}
produce byte-identical models.
}

\section{Cross-validation}{

10-fold CV by \code{glmnet::cv.glmnet()} over the lambda path; AUC
is recomputed per fold for the model card. Class imbalance is
not reweighted by default (use the \code{weights} arg if needed).
}

\examples{
if (requireNamespace("glmnet", quietly = TRUE)) {
  set.seed(1)
  feats <- data.frame(gene_symbol = paste0("G", seq_len(30)),
                      x1 = rnorm(30), x2 = rnorm(30))
  y <- rep(c(0L, 1L), each = 15)
  fit_user_evidence_model(feats, y, nfolds = 3L)
}

\donttest{
  if (requireNamespace("glmnet", quietly = TRUE)) {
    feats <- make_evidence_features(scale = "z",
                                      drop_na_rows = TRUE)
    y <- as.integer(feats$gene_symbol \%in\%
                      c("LGALS3BP", "TIMP1", "LRG1"))
    fit <- fit_user_evidence_model(feats, y)
    explain_user_evidence_model(fit, top_n = 5)
  }
}
}
\seealso{
\code{\link[=predict_user_evidence_model]{predict_user_evidence_model()}},
\code{\link[=explain_user_evidence_model]{explain_user_evidence_model()}}, \code{\link[=model_card]{model_card()}},
\code{\link[=make_evidence_features]{make_evidence_features()}}.
}
